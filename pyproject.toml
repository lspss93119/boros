[project]
name = "boros-research"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "duckdb>=1.0,<2",
  "pyarrow>=15",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
